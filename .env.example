# Only RPC_URL is needed locally; addresses live in config/chain.json (committed, shared with CI).
# Public RPC is rate-limited — use Alchemy/QuickNode for sustained collection.
RPC_URL=https://rpc.mainnet.chain.robinhood.com
